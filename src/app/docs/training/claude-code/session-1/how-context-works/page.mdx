import { Callout } from "@/components/Callout";

import { ReadTime } from "@/components/ReadTime";
export const metadata = {
  title: "How Context Works",
};

# How Context Works

<ReadTime minutes={2} />

Everything Claude knows about your task must fit in one context window. There is no hidden memory between messages — the window is all there is.

## What counts as context

| Source                  | Description                                   |
| ----------------------- | --------------------------------------------- |
| **Your messages**       | Every prompt you've sent in this conversation |
| **Claude's responses**  | All previous outputs in the thread            |
| **Uploaded files**      | Documents, configs, logs you've attached      |
| **Tool outputs**        | Results from web search, plugins, connectors  |
| **System instructions** | Preferences, memory, project context          |

<br />

<Callout variant="warning" title="Keep it fresh" align="left">
  Long conversations degrade quality. When things get muddled, start a fresh
  conversation.
</Callout>

## Where to go next

- **[Context Window & Tokens](/docs/training/claude-code/session-1/context-window-tokens)** — the math behind why fresh sessions matter
- **[Thinking Effort, Models, & Quotas](/docs/training/claude-code/session-1/thinking-effort-quotas)** — match model and depth to the task
