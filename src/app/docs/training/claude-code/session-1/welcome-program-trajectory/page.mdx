import { TrainingRoadmap } from "@/components/TrainingRoadmap";

export const metadata = {
  title: "The Five-Session Journey",
};

# The Five-Session Journey

From manual task execution to autonomous workflows with guardrails.

<TrainingRoadmap activeStep={1} />
