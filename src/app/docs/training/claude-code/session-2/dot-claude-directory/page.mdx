import { ProjectConfig } from "@/components/ProjectConfig";

import { ReadTime } from "@/components/ReadTime";
export const metadata = {
  title: "The .claude/ Directory",
};

# Project Configuration

<ReadTime minutes={3} />

Two locations — repo root files and the `.claude/` directory.

<ProjectConfig />

## Where to go next

- **[Hooks: Lifecycle & Control](/docs/training/claude-code/session-2/hooks-lifecycle-control)** — deterministic automation at every stage of a session
- **[Skills, Plugins & Connectors](/docs/training/claude-code/session-2/skills-plugins)** — per-project capabilities, subagents, and agent teams
