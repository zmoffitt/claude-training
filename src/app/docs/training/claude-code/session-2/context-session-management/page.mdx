import { ContextSession } from "@/components/ContextSession";

import { ReadTime } from "@/components/ReadTime";
export const metadata = {
  title: "Context & Session Management",
};

# 1M Context & Session Management

<ReadTime minutes={3} />

Your context window is finite — manage it deliberately.

<ContextSession />

## Where to go next

- **[CLAUDE.md](/docs/training/claude-code/session-2/claude-md)** — pin durable context so you don't keep retyping it
- **[Hooks: Lifecycle & Control](/docs/training/claude-code/session-2/hooks-lifecycle-control)** — deterministic automation around the session
