import { PermissionModes } from "@/components/PermissionModes";

import { ReadTime } from "@/components/ReadTime";
export const metadata = {
  title: "Permission Modes & Settings",
};

# Permission Modes + Settings

<ReadTime minutes={3} />

Choose your comfort level — settings cascade through layers.

<PermissionModes />

## Where to go next

- **[Working Directories & Scope](/docs/training/claude-code/session-2/working-directories-scope)** — Claude operates from where you launch it; scope tight
- **[Context & Session Management](/docs/training/claude-code/session-2/context-session-management)** — branching points, fresh sessions, the 1M window
